
{{ form_start(form) }}
{{ form_row(form._token) }}
<div class="form-group">
    {{ form_widget(form.content, {attr: {class: 'form-control'}}) }}
</div>
<button class="btn btn-success">Leave comment</button>
{{ form_end(form) }}
