
{{ form_start(form) }}
{{ form_row(form._token) }}
<div class="form-group">
    {{ form_widget(form.content, {attr: {class: 'form-control'}}) }}
</div>
<button class="btn btn-success mb-3">Laissez votre commantaire</button>
{{ form_end(form) }}
